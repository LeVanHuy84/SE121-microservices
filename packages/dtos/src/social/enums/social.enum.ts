export enum ReactionType {
    LIKE,
    LOVE,
    HAHA,
    WOW,
    SAD,
    ANGRY,
}

export enum Audience {
    PUBLIC,
    FRIENDS,
    ONLY_ME,
}

export enum PostStatus {
    ACTIVE,
    DELETED,
    HIDDEN,
}