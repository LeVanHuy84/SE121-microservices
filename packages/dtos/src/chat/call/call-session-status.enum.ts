export enum CallSessionStatus {
  INITIATED = 'initiated',
  RINGING = 'ringing',
  ACCEPTED = 'accepted',
  ENDED = 'ended',
  REJECTED = 'rejected',
  MISSED = 'missed',
  CANCELLED = 'cancelled',
}
